export interface IDashboardTemplateProps {
  /**
   * Setting this to `true` will show the outlines of where the elements
   * should go, this should usually only be used in Storybook!
   *
   * @default `false`
   */
  showOutlines?: boolean;
}
