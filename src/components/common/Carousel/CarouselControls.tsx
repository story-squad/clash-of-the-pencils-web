import React from 'react';
import { CarouselControlsProps } from './CarouselTypes';

const CarouselControls = (props: CarouselControlsProps): React.ReactElement => {
  return (
    <div className="carousel-controls">
      <p>CONTROLS</p>
    </div>
  );
};

export default CarouselControls;
