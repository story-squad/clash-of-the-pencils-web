import React from 'react';
import RenderSubmissionPage from './RenderSubmissionPage';

const SubmissionPageContainer = (): React.ReactElement => {
  return <RenderSubmissionPage />;
};

export default SubmissionPageContainer;
