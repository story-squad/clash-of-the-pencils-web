import React from 'react';
import { Header } from '../../common';

const RenderStreamPage = (): React.ReactElement => {
  return (
    <>
      <Header />
      <div className="stream-page"></div>
    </>
  );
};

export default RenderStreamPage;
