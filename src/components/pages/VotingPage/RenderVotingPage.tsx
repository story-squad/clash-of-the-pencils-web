import React from 'react';
import { Header } from '../../common';

const RenderVotingPage = (): React.ReactElement => {
  return (
    <>
      <Header />
      <div className="voting-page"></div>
    </>
  );
};

export default RenderVotingPage;
